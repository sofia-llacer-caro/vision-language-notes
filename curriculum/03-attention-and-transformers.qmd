---
title: "Stage 3: Attention and Transformers"
date: today
---

## Reading
3Blue1Brown introduction to attention in tansformers as a first foot in.
[https://www.youtube.com/watch?v=eMlx5fFNoYc](https://www.youtube.com/watch?v=eMlx5fFNoYc)

Vaswani et al. 2017, *Attention is All You Need*.
[https://arxiv.org/abs/1706.03762](https://arxiv.org/abs/1706.03762)

Jay Alammar, *The Illustrated Transformer*.
[https://jalammar.github.io/illustrated-transformer](https://jalammar.github.io/illustrated-transformer)

Start with the Illustrated Transformer, it is much more accessible than the paper. 
Then read the paper to see the formal treatment.

Estimated time: 4-5 hours.

## Key concepts to watch for

- The attention mechanism: what it is computing and why it is useful
- Query, Key, Value: what these three vectors represent and where they come from
- Self-attention: how tokens attend to each other within a sequence
- Multi-head attention: why you want several attention mechanisms running in parallel
- Positional encoding: why it is needed and how it works
- Vision Transformers (ViTs): how the transformer architecture is adapted for images

## Checkpoint

After finishing the reading, answer these in your own words:

1. What is the attention mechanism computing? What is the intuition behind 
Query, Key, and Value?
2. Why is self-attention more powerful than convolution for capturing 
long-range dependencies?
3. What is multi-head attention and why is it useful?
4. CNNs have translation equivariance built in by design. 
Transformers do not: how do they compensate for this?
5. How does a Vision Transformer (ViT) adapt the transformer architecture 
for images? What is the equivalent of a "token" in this setting?

*(answers below)*

## My answers

*(to be filled after reading)*

## Open questions
- **How is softmax mathematically described? What function is it?;**

## Notes

### From 3Blue1Brown Video

The goal of a transformer is to take in a piece of text and produce a probability of the next word. For this, we first need to produce embeddings of given words, which are a vector representation of semantic meaning of a word. Treansformers can serve as to encode increasingly complex meaning. For instance, given the polysemantic word "mole", one might produce a generic embedding which can then be changed given context, specifying particular meanings, which is possible due to attention. More generally, attention can vary the embedding of a word based on previous context within the text.

Given an example such as [adjective][noun], the aim is to have the embedding of [noun] incorporate [adjective] (we would have one attention head here). The adjectives for [noun] are encoded in a vector called query, which has, say, 128 dimensions. There is another operation which you perform on the embeddings of the individual words, called the keys. These are effectively answering the question the other way around: [adjective] is [noun]. This way, id you compute the dot product between queries and keys, you can obtain a sort of similarity matrix to where they align. Meaning the embeddings of [adjective] attend to [noun], in case the value for the dot product matric element is high. If you were to choose a more general word, such as "the", and any [noun], the value here should be low, since they are not per se related. We can then multiply the embedding of a given word by the relevance component for each of the previous word. This way, we would be updating the embedding of such word to encode information which was previously written. Note that the probabilities or relevances in this dot product matrix can range from $-\infty$ to $+\infty$, so we want to normalise them to lie between 0 and 1 through a softmax. We call this grid or dot product the "attention pattern". Note as well that, since the matrix of attention pattern includes both words that come before but also those which come after, for training purposes especially, we make sure that they are not visible as to not "give away" what word actually comes after. The way this is done is by an operation called masking, where you set those values to effectively $-\infty$, so that when you apply softmax the values become 0. This attention pattern matrix scales with square of the context window, which can turn computationally heavy very easily and therefore is a bottleneck in LLMs, which is why it is an ongoing subject of study.

For the purposes of one-headed attention, we update the embedding of a term thorugh the value vector, which you multiply by the original term embedding. You take the previous embedding of the context-free word and mutiply it by the value vector in order to obtain a context-rich updated meaning of the original word. This would correspond to a single head of attention. All this process consists of 3 parameter matrices: query, key and value. All of them are filled with tunable parameters, which would sum up to around 150 million parameters in a conservative estimate. In order to make this more computationally efficient, we can break the value matrix into two matrices through matrix factorisation. Now, we instead have around 6 million parameters instead. Attention heads we have been talking about so far are self-attention (used in GPTs), not cross-attention (used in tasks such as translation).

In real applications, though, like GPT-3, attention is multi-headed, and is computed in 96 different layers in parallel. One can think of it as a computation of how context can affect the next word in many different ways. Furthermore, the way that a GPT is structured is in attention, then multilayer perceprtrons, and viceversa on repeat again. The further down the netowrk you go, the more nuanced you get and higher abstraction in meaning you get. The total parameters in GPT are around 57 billion out of the total 175 billion parameters that compose the model, which come mostly from the multilayer perceptron. Since increasing parameters and scale matters a lot in performance, attention is an architecture which allows for a parallelisable architecture which is more scalable.

### From Google Explaining Transformers Video


Transformers are a type of neural network architecture. CNNs for instance are adequate for image processing, but we didn't have anything near comparable in the case of text analysis. A recurrent neural network (RNN) was the way which it was done before transformers. It use to be a sequential analysis, for the order matters a lot in language. However, they couldn't paralelise well since they are sequential analysis architectures and they also did not perform well in extensive pieces of text. Transformers, on the other hand, are very paralleliseable.

The core innovations of transformers are the following: positional encoding and attention (specially self-attention).

#### Positional Encoding
Instead of looking at words sequentially, you store information about word order in the data itself, incase of it just being the way it is computed.

#### (Self-) Attention
Disambiguing text meaning in polysemantic words or storing information from particular nouns within a text.